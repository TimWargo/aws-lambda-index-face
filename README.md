# aws-lambda-index-face

run the make commands to build and zip up the function

upload the zip file to the index face lambda function in the AWS console

Handler should be set to bootstrap by default